// SPDX-License-Identifier: MIT
pragma solidity ^0.8.11;
import "./users.sol";

// The skeleton.sol works as the structure out of which all functions and users can be declared


/*
contract autoBot {
    //This is the structure for creating every indivudaul car. Here the user can see the name, status, money owed, etc. Of the car.      
    struct Car {
        string name;
        uint distanceUntilService;
        uint status;
        uint totalTimeDriven;
        uint totalMilesDriven;
        uint timeStart;
        uint timeStop;
        uint milesDriven;
        uint totalMoney;
    }

    address public autoBot_;
    uint price = 0;
    uint distance;
    
    //Users array to store the user objects in
    User[] users;

    //Cars array to store the car objects in
    Car[] cars;

    constructor() {}

    //To create a new car
    function addCar(string memory name_) public {}

    //To add a new User
    function addUser(string memory password_) public {}}

    //Ordering a ride
    function summonRide(uint userNumber, string memory password, uint car, uint distance_) public {}
   
   //Cost of the ride before and after ride
    function cost(uint miles_) public {}

    //Display the vehicle status 
    function displayVehicleStatus() public view returns () {}

    //Display next service 
    function displayNextService(uint car) public view returns () {}

    //Displaying total distance, money, time driven
    function displayTotals(uint car) public view returns () {}

    //Display user profiles 
    function displayUserProfiles() public view returns () {}

    //Displaying the cars profile
    function displayCarProfiles() public view returns () {}
}
*/